language: cpp
sudo: required
dist: trusty # modern upx

env:
  matrix:
    - BATTLESHIP_TARGET=linux FILENAME_SUFFIX='linux64'
    - BATTLESHIP_TARGET=windows FILENAME_SUFFIX='windows32'
    - BATTLESHIP_TARGET=windows FILENAME_SUFFIX='windows64'

before_install:
  - sudo apt-get update
  - ./build/linux/requirements.sh
  - ./build/${BATTLESHIP_TARGET}/requirements.sh

install:
  - source ./build/${BATTLESHIP_TARGET}/build.sh

script:
  - echo "Nothing."

before_deploy:
  - source ./build/${BATTLESHIP_TARGET}/package.sh

deploy:
  provider: releases
  api_key: "$GH_API_KEY"
  file: "$(ls battleship_*_*.exe)"
  skip_cleanup: true
  on:
    tags: true
